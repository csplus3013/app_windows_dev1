TODO: this is empty  `Dot NET 8 project` dotnet new winforms -n MySimpleApp.
I would like you to create simple APP in C#. Main goal of it is: customing buttons with functionality. When tap on button it runs unility from command line with agruments.
- Layout the App, on top, main menu bar which includes - 1. create a new button with parameters of it. 2. edit button parameters. 3. save settings. Than, below the main menu bar a window which separeted for two sides, left,right. after all side windows a new window log output, it shows everything out from utility. 

- Parameter of custom button utility, 1.name of it, 2. utility to run,will choose location from local PC. 2. arguments for utility.

- below main menu bar on the left side a button with workflow - Upload files from local PC, all uploaded files appears as a list in the window. 

- below main menu bar on the right side apprears all custom buttons, Workflow - When press on the one of custom buttons runs utility within those file,files.


1. utility to clean metadata called `exiftool`, setup where is location. 
can be run with commands: 
- `clean  FLUX trace` -> exiftool -Workflow= -Prompt= -overwrite_original $file
- `show all data` ->   exiftool -G1 -a -s $file
- `clean all` exiftool -jumbf:all= $file
TODO: this is new `Dot NET 8 project`  I would like you to create simple in C# app.  it is window base app with functions, upload files than run unitily from command line. 
an app has separated sections within a window,  top - uplaod file, unility choose, long log output.   
1. utility to clean metadata called `exiftool`, setup where is location. 
can be run with commands: 
- `clean  FLUX trace` -> exiftool -Workflow= -Prompt= -overwrite_original $file
- `show all data` ->   exiftool -G1 -a -s $file
- `clean all` exiftool -jumbf:all= $file
